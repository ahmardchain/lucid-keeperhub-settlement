# Live evidence artifacts

`npm run demo:live` writes `receipts.json` here only after every recorded KeeperHub execution has an independently verified successful receipt.

The repository does not ship fixture hashes under the live filename. Generate the funded Base Sepolia evidence, inspect every transaction on BaseScan, then commit the resulting file for the hackathon submission.
